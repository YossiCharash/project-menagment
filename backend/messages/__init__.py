"""User-facing message classes, organised one subfolder per domain topic.

Every class defined under ``backend.messages`` contains only user-visible
strings (Hebrew UI text, English validation text, audit descriptions shown to
users). Non-visible domain constants (enum values, technical identifiers)
belong with their service, not here.
"""
